service: serverless-project

provider:
  name: aws
  runtime: nodejs6.10

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: /
          method: GET

plugins:
  - serverless-offline